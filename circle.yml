machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  services:
    - docker

dependencies:
  pre:
    - sudo pip install docker-compose

test:
  override:
    - mvn clean install
    - docker-compose up -d

test:
  post:
      - mkdir -p $CIRCLE_TEST_REPORTS/junit/
      - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;

deployment:
  hub:
    branch: /.*/
    commands:
      - docker login -e $DOCKER_HUB_EMAIL -u $DOCKER_HUB_USER_ID -p $DOCKER_HUB_PWD
      - docker tag beerrestservice_spring-rest $DOCKER_HUB_USER_ID/beerrestservice:$CIRCLE_SHA1
      - docker tag beerrestservice_spring-rest $DOCKER_HUB_USER_ID/beerrestservice:latest
      - docker push $DOCKER_HUB_USER_ID/beerrestservice:$CIRCLE_SHA1
      - docker push $DOCKER_HUB_USER_ID/beerrestservice:latest